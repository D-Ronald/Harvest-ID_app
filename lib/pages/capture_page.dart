import 'package:flutter/material.dart';
import 'package:debug_no_cell/utils/base.dart';

class CapturPage extends StatelessWidget {
  const CapturPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: white_base,
      )
    );
  }
}